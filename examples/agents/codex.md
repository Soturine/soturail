# Codex Example

```bash
soturail agents export --agent codex
soturail context pack --target codex
```

Use the generated `AGENTS.md` and context pack as project-local guidance. Do not route `git push` through `soturail run`.
