# Cursor Example

```bash
soturail agents export --agent cursor
soturail agents install --agent cursor --mode rules --dry-run
```

Cursor rule exports are project-local and should be reviewed before writing to `.cursor/rules/`.
